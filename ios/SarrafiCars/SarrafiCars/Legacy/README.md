# Legacy (not compiled)

Old web-shell tab prototypes (`InventoryTabView`, `AccountTabView`, etc.). Kept for reference only. The shipping app uses `App/MainTabView.swift` and `Features/*`.

Do not add these files to the Xcode target without an explicit product decision.

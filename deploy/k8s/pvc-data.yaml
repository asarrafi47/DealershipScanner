apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dealership-data-pvc
  namespace: dealership
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi

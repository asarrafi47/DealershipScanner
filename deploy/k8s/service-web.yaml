apiVersion: v1
kind: Service
metadata:
  name: dealership-web
  namespace: dealership
spec:
  selector:
    app: dealership-web
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP

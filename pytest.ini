[pytest]
testpaths = backend/tests
markers =
    integration: tests that require a live running stack (HTTP server, external services)
    regression: scanner/pipeline regression tests gated separately in CI
